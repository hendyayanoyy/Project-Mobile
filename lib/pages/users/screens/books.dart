import 'package:flutter/material.dart';
import 'package:project_akhir/components/list_book.dart';

class Books extends StatelessWidget {
  const Books({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListBooks();
  }
}